{
  "name": "pi-extension-sandbox",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "echo 'nothing to clean'",
    "build": "echo 'nothing to build'",
    "check": "node --check index.ts && node --experimental-strip-types -e \"Promise.all([import('./policy.ts'), import('./approval-broker.ts'), import('./lifecycle.ts'), import('./runtime.ts')])\" && node tests/check.mjs && node tests/approval-broker.check.mjs && node tests/lifecycle.check.mjs"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "dependencies": {
    "@anthropic-ai/sandbox-runtime": "0.0.71"
  }
}
